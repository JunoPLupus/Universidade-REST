generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Os models (Curso, Disciplina, Semestre, Professor, Admin, Lecionamento, ...)
// serão adicionados aqui conforme as entidades forem implementadas.
